# LLM 配置
# 请选择一种 LLM 提供商并配置相应的 API Key

# Azure OpenAI 配置（推荐）
AZURE_OPENAI_API_KEY=your_azure_openai_api_key
AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=gpt-35-turbo
AZURE_OPENAI_API_VERSION=2024-02-15-preview

# 标准 OpenAI 配置（可选）
OPENAI_API_KEY=your_openai_api_key

# Anthropic 配置（可选）
ANTHROPIC_API_KEY=your_anthropic_api_key

# 硅基流动配置（可选）
SILICONFLOW_API_KEY=your_siliconflow_api_key
SILICONFLOW_MODEL=deepseek-chat
SILICONFLOW_BASE_URL=https://api.siliconflow.cn/v1

# 注意：如果没有配置任何 API Key，应用将使用降级模式运行

# 应用配置
PORT=3000
PYTHONPATH=/app

# 管理员账户配置（可选，使用默认值）
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin
ADMIN_DISPLAY_NAME=系统管理员

# 数据库配置
# SQLite 文件路径（已删除 data 目录，使用 PostgreSQL）
# DATABASE_PATH=/app/data/tasks.db

# PostgreSQL 配置（用于记忆存储）
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=ai_todo_db
POSTGRES_USER=ai_todo_user
POSTGRES_PASSWORD=ai_todo_password

# --------------------
# 嵌入服务配置
# 选择使用的嵌入提供商: openai, cohere, huggingface
# --------------------
EMBEDDING_PROVIDER=openai

# --------------------
# OpenAI 嵌入配置
# --------------------
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# --------------------
# Cohere 嵌入配置
# --------------------
COHERE_API_KEY=
COHERE_EMBEDDING_MODEL=embed-multilingual-v2.0

# --------------------
# Hugging Face 嵌入配置
# --------------------
HF_MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2
HUGGINGFACE_API_KEY=
